//{{ template }}

public class {{ name }}_Generated {
	{{for property in properties}} 
	{{for attribute in property.attributes}}[{{ attribute.name }}]{{end}}
	private object {{ property.name }};
	{{end}}
}